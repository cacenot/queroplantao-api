"""Job postings module routes."""

from fastapi import APIRouter

router = APIRouter(prefix="/job-postings", tags=["Job Postings"])


# TODO: Add job posting endpoints
# @router.get("/")
# @router.get("/{job_posting_id}")
# @router.post("/")
# @router.put("/{job_posting_id}")
# @router.delete("/{job_posting_id}")

# TODO: Add application endpoints
# @router.post("/{job_posting_id}/apply")
# @router.get("/{job_posting_id}/applications")
