"""Schedules module routes."""

from fastapi import APIRouter

router = APIRouter(prefix="/schedules", tags=["Schedules"])


# TODO: Add schedule endpoints
# @router.get("/")
# @router.get("/{schedule_id}")
# @router.post("/")
# @router.put("/{schedule_id}")
# @router.delete("/{schedule_id}")
