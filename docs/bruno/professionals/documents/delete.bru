meta {
  name: Delete Document
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/professionals/:professionalId/documents/:documentId
  body: none
  auth: inherit
}

params:path {
  professionalId: {{professionalId}}
  documentId: {{documentId}}
}

docs {
  # Delete Document
  
  Remove um documento do profissional (soft delete).
  
  ## Error Codes
  
  | Status | Code | Descrição |
  |--------|------|-----------|
  | 404 | `PROF_DOCUMENT_NOT_FOUND` | Documento não encontrado |
}
